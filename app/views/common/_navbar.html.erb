<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Q Store", root_path, :class => "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
<!--         <li class="active"><a href="#">Link</a></li>
        <li><a href="#">Link</a></li> -->
        <li> <%= link_to("過去訂單", account_orders_path) %> </li>
        <li class="dropdown">
          <% if is_admin? %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Manage<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "All Products", admin_products_path %></li>
            <li><%= link_to "All Orders", admin_orders_path %></li>
            <li><%= link_to "Add Product", new_admin_product_path %></li>
          </ul>
          <% end %>
        </li>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <li>     
          <%= search_form_for @search, url: search_products_path, :html => {method: 'post', class: 'navbar-form'} do |f| %>
          <div class="form-group">
            <%= f.search_field :title_cont, class: 'form-control', placeholder: "找產品" %>
            <%= button_tag class: 'btn btn-info' do %>
            <span class="glyphicon glyphicon-search"></span>
            <% end %>
          </div>
          <% end %>


        </li>
        <li> 
          <%= link_to carts_path do  %>
            購物車 <i class="glyphicon glyphicon-shopping-cart"> </i> (<%= cart_items_count(current_cart) %>)
          <% end %>
        </li>
        
          <% if !current_user %>
            <li><%= link_to("登入", new_user_session_path) %></li>
          <% else %>
            <li><%= link_to("登出", destroy_user_session_path, :method => :delete ) %></li>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
